<div class="container" style="padding-top:48px;padding-bottom:64px;max-width:860px;">
  <h1 style="font-family:var(--font-heading);font-size:2.2rem;color:var(--color-primary);margin-bottom:32px;">{{ page.title }}</h1>
  <div style="font-size:.95rem;line-height:1.9;color:var(--color-text);">
    {{ page.content }}
  </div>
</div>
